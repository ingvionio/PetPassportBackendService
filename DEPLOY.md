# 🐳 Развертывание PetPassport через Docker

## Быстрый старт

### 1. Установите Docker и Docker Compose

- [Docker Desktop](https://www.docker.com/products/docker-desktop) (включает Docker Compose)

### 2. Настройте переменные окружения

Откройте `docker-compose.yml` и замените:
- `${POSTGRES_PASSWORD:-your_secure_password_here}` на ваш надежный пароль
- `${BOT_SERVICE_URL:-http://localhost:8000}` на URL вашего сервиса бота

Или создайте файл `.env` в корне проекта:
```env
POSTGRES_PASSWORD=your_secure_password_here
BOT_SERVICE_URL=http://your-bot-service:8000
```

### 3. Запустите проект

```bash
# Из корневой директории проекта
docker-compose up -d
```

### 4. Проверьте работу

- API: http://localhost:8080
- Swagger: http://localhost:8080/swagger (если включен)
- Логи: `docker-compose logs -f api`

## Основные команды

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Просмотр логов
docker-compose logs -f api

# Пересборка после изменений
docker-compose build api
docker-compose up -d api

# Остановка с удалением данных БД
docker-compose down -v
```

## Структура файлов

```
.
├── Dockerfile              # Образ ASP.NET Core приложения
├── docker-compose.yml      # Конфигурация сервисов
├── .dockerignore           # Исключения для Docker
├── README.Docker.md        # Подробная документация
└── PetPassport/           # Исходный код
```

## Важные моменты

1. **База данных**: PostgreSQL запускается в отдельном контейнере
2. **Миграции**: Применяются автоматически при старте приложения
3. **Загрузки**: Файлы сохраняются в `./uploads` (монтируется как volume)
4. **Переменные окружения**: Переопределяют значения из `appsettings.json`

## Production

Для production рекомендуется:
- Использовать внешнюю БД (не в контейнере)
- Настроить HTTPS через reverse proxy (nginx)
- Использовать секреты Docker или внешний менеджер секретов
- Настроить мониторинг

Подробнее см. `README.Docker.md`

